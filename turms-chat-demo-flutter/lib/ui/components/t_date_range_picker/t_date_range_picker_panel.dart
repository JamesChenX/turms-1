part of 't_date_range_picker.dart';

class _TDateRangePickerPanel extends StatelessWidget {
  const _TDateRangePickerPanel({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => Container(
        width: 100,
        height: 100,
        color: Colors.red,
      );
}